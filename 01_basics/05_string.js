const name = 'sachin'
const age= 25

//console.log(`Namaste this is ${name} and my age is ${age}`);

const rich = new String('Yes i`m Billionaire yaa')

//console.log(rich.slice(9,18)); //the end section are not execute but start and position of end`s before index will execute 

//console.log(rich.search('i')); // it return indexof

console.log(rich.repeat('yes')); // not execute